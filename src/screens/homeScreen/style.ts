/* ------------------------------ Basic imports ----------------------------- */
import {StyleSheet} from 'react-native';
/* -------------------------------- Constants ------------------------------- */

const styles = StyleSheet.create({
  homeScreen: {
    width: '100%',
    height: '100%',
    // flex: 1,
  },
});

export default styles;
